generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Workspace {
  id            String    @id
  installation  Json
  apiKey        String?
  scopeVersion  Int       @default(0)
}